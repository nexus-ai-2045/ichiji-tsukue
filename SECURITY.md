# 公開してよいもの、入れてはいけないもの

- API 鍵、トークン、`.env`、接続文字列。実行時の環境変数だけ。リポジトリに書かない。
- クエリ本文、検索結果の本文、他の人のクエリ。残すのはクエリ文字列と〇×と消費だけで、それもこのリポジトリには入れない。
- クライアントが送ったユーザ ID は信じない。資格判定はサーバ側。
- 検索の取り先は許可したホストだけ。ユーザが渡した URL をサーバが開かない。
- 上流が死んだら「出せない」。別の検索へ黙って落ちない。
- フォロー確認が無い状態で、このリポジトリを公開サービスの実装と呼ばない。
